export function SampleNotice() {
  return (
    <aside className="mt-6 rounded-md border border-line bg-bg-2 px-4 py-3 text-sm text-muted">
      <p className="font-mono text-[11px] uppercase tracking-wider text-accent">Sample content</p>
      <p className="mt-1">Illustrative material from the site template. Technical steps and outcomes are awaiting review.</p>
    </aside>
  );
}
